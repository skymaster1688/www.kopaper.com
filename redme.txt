刚刚那条消息在发送时被截断了，我会重新给你一份 **完整、可直接交付给开发团队的开发文档（Detailed Technical Specification + PRD）**。

下面是完整版👇
（内容较长，但结构清晰，每个模块都能直接执行。）

---

# 📘 **Kopaper.com – 完整开发文档（PRD + 技术文档 + UI 流程 + API 说明）**

---

# **1. 产品定位（Product Positioning）**

**Kopaper.com 是一款基于 AI 的文本 Humanizer（人性化改写工具）**
主要功能：

* 输入英文文本
* 改写为自然、人类风格、流畅的英文
* 保留原意，提升可读性、语气、逻辑结构
* 提供多种风格（Casual / Professional / Academic / Friendly）

不涉及 AI 检测规避，仅改善写作质量。

---

# **2. 系统模块结构（System Architecture Overview）**

```
Frontend (Next.js + Tailwind)
     |
     | API (REST)
     ↓
Backend (Node.js / Python)
     |
     |--- AI Engine (OpenAI API)
     |--- Text Processing (Tone, Rewrite, Grammar)
     |--- Usage Control (Rate Limit / Plan Checking)
     |--- Auth System
     ↓
Database (PostgreSQL / Supabase)
     |
     ├─ Users
     ├─ Billing (Stripe)
     ├─ History logs (可选)
     └─ API keys / settings
```

---

# **3. 页面结构（Sitemap）**

### **Required**

* `/` 首页
* `/humanizer` 主工具页
* `/pricing`
* `/login`
* `/signup`
* `/dashboard`
* `/history`（可选）
* `/blog`
* `/blog/[slug]`
* `/privacy`
* `/terms`
* `/api`（文档页）

---

# **4. 首页需求文档（Home Page PRD）**

**主要目的：转化用户 → 点击 “Humanize Your Text”**

组件结构：

1. **Hero Section**

   * 主标题：Turn AI Text Into Natural, Human-Friendly Writing
   * CTA 按钮（跳转到 /humanizer）

2. **Feature Section**

   * 6 个 Feature
   * 图标 + 简单描述

3. **How It Works**

   * 3 steps 图示

4. **Example (Before / After)**

   * 用户能直观看到效果

5. **Use Cases**

   * Essay / Email / Blog / Business Documents

6. **SEO Text（350–500 词）**

   * 用于提升首页排名

7. **Footer**

---

# **5. 主工具页（Humanizer Page PRD）**

## 5.1 核心界面布局

```
----------------------------------------------
| Header                                    |
----------------------------------------------
| Left Column: Text Input Box               |
| Right Column: Output Box                  |
----------------------------------------------
| Bottom Controls:                          |
| - Select Tone                             |
| - Rewrite Button                          |
| - Clear / Copy                            |
----------------------------------------------
```

## 5.2 输入限制

* 免费用户：最多 300 字符
* Pro：最多 8000–12000 字符（可配置）

## 5.3 Tone 选项

* Neutral
* Professional
* Casual
* Friendly
* Academic

## 5.4 API 返回结构

```
{
  "status": "ok",
  "input_length": 323,
  "output": "rewritten text ...",
  "tone": "professional"
}
```

---

# **6. 用户系统（Auth）**

使用 **Supabase Auth 或 NextAuth + PostgreSQL**

## 核心功能：

* Signup / Login（Email + Password）
* OAuth（Google）
* 重置密码
* 用户 Dashboard 页面

---

# **7. 订阅系统（Billing System）**

使用 **Stripe Subscription**

### 订阅方案：

#### **Free**

* Daily rewrites: 10 次
* Max length: 1500 字符

#### **Pro**

* Unlimited rewrites
* Max length: 12000 字符
* All tones
* Priority API speed

---

# **8. 数据库结构（DB Schema）**

## `users`

| id | email | password_hash | stripe_customer_id | created_at |

## `plans`

| id | user_id | plan_type (free/pro) | expire_date |

## `rewrite_logs`

| id | user_id | input | output | tone | created_at |

## `api_keys`（可选）

| key | user_id | rate_limit |

---

# **9. AI 引擎（Humanizer Algorithm）**

使用 OpenAI API：

### Prompt 示例（核心逻辑）：

```
Rewrite the following text into natural, human-like, fluent English.
Do not change meaning. Improve clarity, transitions, and tone.
Target tone: {{tone}}.
Text: """{{text}}"""
```

### 模型建议

* gpt-4.1-mini 便宜且质量足够
* 输出质量特别高可换 gpt-4.1 或 gpt-5.1-mini

---

# **10. API 路由（Backend）**

### `POST /api/rewrite`

**Request**

```
{
  "text": "...",
  "tone": "professional",
  "user_id": "123"
}
```

**Response**

```
{
  "output": "rewritten text...",
  "usage": 321
}
```

### `GET /api/user/plan`

### `POST /api/upgrade`

---

# **11. 前端开发要求（Next.js）**

### UI 技术栈

* Next.js 14 (App Router)
* TailwindCSS
* shadcn/ui 组件
* Zustand（状态管理）
* React Query（API 数据）

### 前端任务列表

* [ ] 首页 UI 完成
* [ ] 人性化工具页
* [ ] Dashboard 页面
* [ ] History（可选）
* [ ] Pricing 页面
* [ ] Blog 系统
* [ ] Login / Signup

---

# **12. SEO 任务清单**

### 首页 SEO

* Title: AI Humanizer – Natural English Rewriter | Kopaper
* Meta Description:
  “Rewrite AI or robotic text into smooth, natural, human-style English with Kopaper’s AI Humanizer tool.”

### URL 规划：
首页 www.kopaper.com

* /ai-humanizer
* /text-humanizer
* /rewrite-ai-text
* /make-text-sound-human

### 博客文章（100 篇内容矩阵）

我可帮你直接生成。

---

# **13. 测试（QA）要求**

### 功能测试

* 文本长度判断
* Tone 切换
* Rewrite 功能
* 登录 / 注册
* 订阅流程
* API 错误处理

### 性能测试

* API 响应 < 2 秒
* 大段文本处理不崩溃

---

# **14. 部署方案**

### 使用 Vercel

* 前端 & Next.js → Vercel
* 后端 API → 使用 Next.js server actions / API Routes
* 数据库 → Supabase（Postgres）
* Stripe → Webhook 部署在 Vercel

